#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "Pate_a_tartiner.h"




short int 
{
    fichier = fopen("Pate_a_tartiner", "r");



















}














/*typedef struct
{
   char *   nom;
   char *   prix;
   char *   label;
   char *   huile_palme;
   int      code;
}
contact_infos;*/


/*
int main(int argc, char *argv[])
{

    

    char choix = scanf("%d", &i);
    if choix == "1"
    {
	



typedef struct cFileCSV 
{ 
	int iRow; /* Numéro de la ligne */ 
/*	int iCol; /* Numéro de la Colonne */ 
/*	char *cColumnName; /* Nom de la colonne */ 
/*	char *cValues; /* Valeurs de la colonne*/ 

/*	struct cFileCSV *next; /* Pointeur sur l'élément suivant. * }cFieldLine;

/*
static char *getSplitFieldWithDelimiter(char **stringp, char delimiter) 
{ 
	char *s, *tok = *stringp; char c; 
	char cEndString = '\ '; 
	char *Ch1=NULL; int l,i=0; l=strlen(tok); 
	if ((tok == NULL) || (l ==0)) 
		return NULL; Ch1=(char*)malloc(sizeof(char)); 
	for (s = tok;(c = *s);s++) 
	{ 
		if ((delimiter == c)||(cEndString == c)) 
		{ 
			*s = 0; *stringp = s+1; return tok; 
		} 
	} 
	*stringp = NULL; return tok; 
} 
*/
